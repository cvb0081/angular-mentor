# CONFIG — your learning setup

This file is the **source of truth** for your journey. Fill the slots below. The AI reads this to tailor everything. Run `SETUP.md` to have the AI interview you and fill it, or edit by hand.

> Lines in `<...>` are slots to replace. Examples come from the original Angular journey this template was extracted from — overwrite them with your own.

## Target

- **What you're learning:** `<language / framework>` — _e.g. Angular_
- **Scope:** `<what's in / out>` — _e.g. frontend-only; capstone uses a mock API_

## You

- **Background / strengths:** `<where you're already strong>` — _e.g. backend-strong_
- **Weak spots:** `<where you're weak>` — _e.g. frontend-weak_
- **Goal:** `<the outcome you want>` — _e.g. confidently put Angular + web fundamentals on the resume via hands-on work_

## Capstone

- **Domain / app idea:** `<the real app you'll build as you learn>` — chosen early, evolves over time.

## Preferences

- **Mentor stance:** `<how much hand-holding>` — _default: explanation over finished code; hints before answers; let me struggle productively._
- **Session cadence / pace:** `<optional>`
- **Anything else the mentor should know:** `<optional>`
